---
import type { HTMLAttributes } from "astro/types";

import ArticleProse from "../articles/ArticleProse.astro";

/** Compatibility props for the legacy article prose import path. */
interface Props extends HTMLAttributes<"div"> {}

const attrs = Astro.props;
---

<ArticleProse {...attrs}>
  <slot />
</ArticleProse>
