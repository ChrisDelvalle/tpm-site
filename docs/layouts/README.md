# Layout Design One-Pagers

This directory documents route-facing Astro layouts in `src/layouts/`.

Layout one-pagers follow the same design-contract style as component
one-pagers, but they are not included in the `docs/components/` mirror because
layouts own page shell orchestration rather than reusable component slots.
