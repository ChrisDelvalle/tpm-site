# Deferred Design Notes

This directory holds design notes for components or features that are not
currently implemented.

Keep these files out of the active component inventory until the matching source
file exists. When deferred work becomes active, move the design note into the
appropriate docs area and add the implementation milestone to `CHECKLIST.md`.
